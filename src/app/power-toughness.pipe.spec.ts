import { PowerToughnessPipe } from './power-toughness.pipe';

describe('PowerToughnessPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerToughnessPipe();
    expect(pipe).toBeTruthy();
  });
});
